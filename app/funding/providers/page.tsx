import {fundingProviders} from "@/lib/funding/data";import {ProviderCard} from "@/components/funding/ProviderCard";export const metadata={title:"Funding Provider Directory | Distilled Funding",description:"Explore funding providers by product coverage, borrower profile, industries and high-level qualification guides."};export default function Page(){return <main className="mx-auto max-w-7xl px-6 py-14"><p className="text-xs font-black uppercase tracking-widest text-blue-700">Capital network</p><h1 className="mt-2 text-5xl font-black">Provider / Partner Directory</h1><p className="mt-4 max-w-3xl text-lg text-slate-600">Explore {fundingProviders.length} providers by financing products, industry appetite and high-level eligibility profile. Internal affiliate economics and submission routes are intentionally excluded.</p><div className="mt-10 grid gap-5 md:grid-cols-2">{fundingProviders.map(p=><ProviderCard key={p.id} provider={p}/>)}</div></main>}
