import Link from "next/link";import type {FundingProduct} from "@/lib/funding/types";import {money} from "@/lib/funding/format";
export function ProductCard({product}:{product:FundingProduct}){return <article className="flex h-full flex-col border-2 border-slate-950 bg-white p-5 shadow-[4px_4px_0_#0f172a]"><div className="text-xs font-black uppercase tracking-widest text-blue-700">{product.fundingType || product.category}</div><h3 className="mt-2 text-xl font-black leading-tight">{product.name}</h3><p className="mt-1 text-sm text-slate-600">{product.providerName}</p><div className="mt-5 grid grid-cols-2 gap-3 text-sm"><div><span className="block text-xs uppercase text-slate-500">Amount</span>{money(product.minAmount)}–{money(product.maxAmount)}</div><div><span className="block text-xs uppercase text-slate-500">Speed</span>{product.timeToFunding || 'Varies'}</div><div><span className="block text-xs uppercase text-slate-500">Min credit</span>{product.minCreditScore ?? 'Varies'}</div><div><span className="block text-xs uppercase text-slate-500">Startup</span>{product.startupEligible?'Yes':'No'}</div></div><Link className="mt-6 inline-flex justify-center bg-slate-950 px-4 py-3 font-bold text-white" href={`/funding/products/${product.slug}`}>View product</Link></article>}
