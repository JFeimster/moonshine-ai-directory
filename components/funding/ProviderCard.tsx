import Link from "next/link";import type {FundingProvider} from "@/lib/funding/types";
export function ProviderCard({provider}:{provider:FundingProvider}){return <article className="border border-slate-300 bg-white p-5"><div className="flex items-start justify-between gap-4"><div><h3 className="text-xl font-black">{provider.name}</h3><p className="mt-1 text-sm text-slate-600">{provider.typicalBorrowerProfile || provider.financingProducts.slice(0,3).join(' · ')}</p></div><span className="shrink-0 bg-slate-100 px-3 py-1 text-xs font-bold">{provider.productCount} products</span></div><div className="mt-4 flex flex-wrap gap-2">{provider.financingProducts.slice(0,5).map(x=><span key={x} className="border px-2 py-1 text-xs">{x}</span>)}</div><Link className="mt-5 inline-block font-bold text-blue-700 underline" href={`/funding/providers/${provider.slug}`}>View provider →</Link></article>}
